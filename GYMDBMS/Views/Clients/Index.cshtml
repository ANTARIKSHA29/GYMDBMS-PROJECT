@*@model IEnumerable<GYMDBMS.Models.Client>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ContactNumer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FitnessGoal)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Trainer)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ContactNumer)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FitnessGoal)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Trainer.TrainerId)
            </td>
                <td>
                    @Html.DisplayFor(modelItem => item.TrainerId)
                </td>

            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
*@



@*@model IEnumerable<GYMDBMS.Models.Client>

@{
    ViewData["Title"] = "Index";
}

<head>
    <!-- Paste the CSS code for the background image and button styles here -->
    <style>
        /* Apply background image to the entire page */
        body {
            background-image: url("/Images/clientsback.jpg");
            background-size: cover; /* Cover the entire viewport */
            background-position: center; /* Center the background image */
            background-repeat: no-repeat; /* Prevent background image from repeating */
            margin: 0; /* Remove default margin */
            padding: 0; /* Remove default padding */
            color: white; /* Set text color to white for better visibility */
        }

        /* Adjust title size and color */
        h1 {
            font-size: 48px; /* Increase title font size */
            color: white; /* Set title text color to white */
        }

        /* Adjust table header and row font size and color */
        th {
            font-weight: bold; /* Make table headers bold */
            font-size:24px;
            color:white;
        }

        td {
            font-size: 24px; /* Increase font size */
            color: white; /* Set text color to white */
        }

        /* Adjust button styles */
        .btn-group {
            display: inline-flex; /* Ensure buttons are in the same line */
        }

        .btn {
            background-color: white; /* Set button background color to white */
            color: black; /* Set button text color to black */
            padding: 10px 20px; /* Increase button padding */
            font-size: 20px; /* Increase button font size */
            border-radius: 5px; /* Add border radius to button */
            border: 1px solid transparent; /* Add border to button */
            transition: all 0.3s ease; /* Add transition effect for smooth hover */
            text-decoration: none; /* Remove underline from button */
            margin-right: 10px; /* Add margin to button */
        }

            /* Hover effect for buttons */
            .btn:hover {
                background-color: #f2f2f2; /* Lighten button background color on hover */
                color: black; /* Change button text color on hover */
            }
    </style>
</head>

<h1>Index</h1>

<p>
    <a asp-action="Create" class="btn">Create New</a> <!-- Apply button styles -->
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ContactNumer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FitnessGoal)
            </th>
            <th>
                Trainer
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ContactNumer)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.FitnessGoal)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Trainer.TrainerId)
                </td>
                <td class="btn-group">
                    @Html.ActionLink("Edit", "Edit", new { id = item.ClientId }, new { @class = "btn" }) |
                    @Html.ActionLink("Details", "Details", new { id = item.ClientId }, new { @class = "btn" }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.ClientId }, new { @class = "btn" })
                </td>
            </tr>
        }
    </tbody>
</table>
*@

@model IEnumerable<GYMDBMS.Models.Client>

@{
    ViewData["Title"] = "Index";
}

<head>
    <!-- Paste the CSS code for the background image and button styles here -->
    <style>
        /* Apply background image to the entire page */
        body {
            background-image: url("/Images/clientsback.jpg");
            background-size: cover; /* Cover the entire viewport */
            background-position: center; /* Center the background image */
            background-repeat: no-repeat; /* Prevent background image from repeating */
            margin: 0; /* Remove default margin */
            padding: 0; /* Remove default padding */
            color: white; /* Set text color to white for better visibility */
        }

        /* Adjust title size and color */
        h1 {
            font-size: 48px; /* Increase title font size */
            color: white; /* Set title text color to white */
        }

        /* Adjust table header and row font size and color */
        th {
            font-weight: bold; /* Make table headers bold */
            font-size: 24px;
            color: white;
        }

        td {
            font-size: 24px; /* Increase font size */
            color: white; /* Set text color to white */
        }

        /* Adjust button styles */
        .btn-group {
            display: inline-flex; /* Ensure buttons are in the same line */
        }

        .btn {
            background-color: white; /* Set button background color to white */
            color: black; /* Set button text color to black */
            padding: 10px 20px; /* Increase button padding */
            font-size: 20px; /* Increase button font size */
            border-radius: 5px; /* Add border radius to button */
            border: 1px solid transparent; /* Add border to button */
            transition: all 0.3s ease; /* Add transition effect for smooth hover */
            text-decoration: none; /* Remove underline from button */
            margin-right: 10px; /* Add margin to button */
        }

            /* Hover effect for buttons */
            .btn:hover {
                background-color: #f2f2f2; /* Lighten button background color on hover */
                color: black; /* Change button text color on hover */
            }
    </style>
</head>

<h1>Index</h1>

<p>
    <a asp-action="Create" class="btn">Create New</a> <!-- Apply button styles -->
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ContactNumer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FitnessGoal)
            </th>
            <th>
                Trainer
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ContactNumer)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.FitnessGoal)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Trainer.TrainerId)
                </td>
                <td class="btn-group">
                    @* Removed the Edit button *@
                    @Html.ActionLink("Details", "Details", new { id = item.ClientId }, new { @class = "btn" }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.ClientId }, new { @class = "btn" })
                </td>
            </tr>
        }
    </tbody>
</table>
