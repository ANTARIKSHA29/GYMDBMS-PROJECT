exercise ka exercise.cshtml

@*@model Exercise

<!DOCTYPE html>
<html>
<head>
    <title>Exercise Details</title>
</head>
<body>
    <div class="exercise">
        <h2>@Model.Name</h2>
        <p>@Model.Instruction</p>
        <img src="~/Images/@(Model.ExerciseId).jpg" alt="@Model.Name" />
    </div>
</body>
</html>
*@
@*@model Exercise

<div class="card">
    <div class="card-header">
        <h2>@Model.Name</h2>
    </div>
    <div class="card-body">
        <p>@Model.Instruction</p>
        <img src="~/Images/@(Model.ExerciseId).jpg" class="card-img-top" alt="@Model.Name" />
    </div>
    <div class="card-footer">
        <div class="btn-group" role="group" aria-label="Exercise actions">
            <a class="btn btn-primary" href="@Url.Action("Details.cshtml", "Exercises", new { id = Model.ExerciseId })">Details</a>
            <a class="btn btn-warning" href="@Url.Action("Edit.cshtml", "Exercises", new { id = Model.ExerciseId })">Edit</a>
            <form asp-action="Delete" asp-route-id="@Model.ExerciseId" method="post" onsubmit="return confirm('Are you sure you want to delete this exercise?');">
                <button type="submit" class="btn btn-danger">Delete</button>
            </form>
        </div>
    </div>
</div>
*@
@*@model Exercise

<!DOCTYPE html>
<html>
<head>
    <title>Exercise Details</title>
    <style>
        /* CSS for setting background image */
        body {
            background-image: url('/wwwroot/Images/exebackground.jpg');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            height: 100vh; /* Ensure the background covers the full height of the viewport */
            margin: 0; /* Remove default margin */
            padding: 0; /* Remove default padding */
        }
        .exercise-content {
            /* Add styles for the content area if needed */
            /* This will ensure that the content is centered on the page */
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
        }
    </style>
</head>
<body>
    <div class="exercise-content">
        <!-- Your exercise content goes here -->
        <h2>@Model.Name</h2>
        <p>@Model.Instruction</p>
        <img src="~/Images/@(Model.ExerciseId).jpg" alt="@Model.Name" />
    </div>
</body>
</html>
*@
@*@model Exercise

<html>
<head>
    <title>Exercise Details</title>
    <style>
        /* CSS for setting background image */
        body {
            background-image: url('@Url.Content("~/Images/exebackground.jpg")');
            background-size: cover;
            background-position: center;
            background-repeat:repeat;
            height: 100vh; /* Ensure the background covers the full height of the viewport */
            font-size: .75em;
            font-family: Verdana, Helvetica, Sans-Serif;
            margin: 0; /* Remove default margin */
            padding: 0; /* Remove default padding */
            color: #696969;
        }

        .exercise-content {
            /* Add styles for the content area if needed */
            /* This will ensure that the content is centered on the page */
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            background-color: rgba(255, 255, 255, 0.8); /* Add a semi-transparent white background for the content */
            padding: 20px; /* Add some padding around the content */
            border-radius: 10px; /* Add rounded corners to the content */
        }
    </style>
</head>
<body>
    <div class="exercise-content">
        <!-- Your exercise content goes here -->
        <h2>@Model.Name</h2>
        <p>@Model.Instruction</p>
        <img src="~/Images/@(Model.ExerciseId).jpg" alt="@Model.Name" />
    </div>
</body>
</html>
*@
@*@model Exercise

<html>
<head>
    <title>Exercise Details</title>
    <style>
        /* CSS for setting background image */
        body {
            background-image: url('@Url.Content("~/Images/exebackground.jpg")');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            height: 100vh; /* Ensure the background covers the full height of the viewport */
            font-size: .75em;
            font-family: Verdana, Helvetica, Sans-Serif;
            margin: 0; /* Remove default margin */
            padding: 0; /* Remove default padding */
            color: #696969;
        }

        .exercise-content {
            /* Add styles for the content area if needed */
            /* This will ensure that the content is centered on the page */
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            background-color: rgba(255, 255, 255, 0.8); /* Add a semi-transparent white background for the content */
            padding: 20px; /* Add some padding around the content */
            border-radius: 10px; /* Add rounded corners to the content */
        }

        .card {
            width: 300px;
            height: 400px;
            background-color: white;
            border-radius: 10px;
            box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
            overflow: hidden;
        }

        .card-image {
            width: 100%;
            height: 200px;
            background-size: cover;
            background-position: center;
        }

        .card-content {
            padding: 10px;
        }

        .card-actions {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0 10px 10px 10px;
        }
    </style>
</head>
<body>
    <div class="exercise-content">
        <div class="card">
            <div class="card-image" style="background-image: url('~/Images/@(Model.ExerciseId).jpg')"></div>
            <div class="card-content">
                <h2>@Model.Name</h2>
                <p>@Model.Instruction</p>
            </div>
            <div class="card-actions">
                <a asp-action="Edit" asp-route-id="@Model.ExerciseId" class="btn btn-primary">Edit</a>
                <a asp-action="Details" asp-route-id="@Model.ExerciseId" class="btn btn-secondary">Details</a>
                <a asp-action="Delete" asp-route-id="@Model.ExerciseId" class="btn btn-danger">Delete</a>
            </div>
        </div>
    </div>
</body>
</html>*@


@model Exercise

<html>
<head>
    <title>Exercise Details</title>
    <style>
        /* CSS for setting background image */
        body {
            background-image: url('@Url.Content("~/Images/exerciseback.jpg")');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            height: 100vh; /* Ensure the background covers the full height of the viewport */
            font-size: .75em;
            font-family: Verdana, Helvetica, Sans-Serif;
            margin: 0; /* Remove default margin */
            padding: 0; /* Remove default padding */
            color: #696969;
        }

        .exercise-content {
            /* Add styles for the content area if needed */
            /* This will ensure that the content is centered on the page */
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            align-items: flex-start;
            padding: 20px;
        }

        .card {
            width: 20%; /* Each card takes up 20% of the container width */
            margin: 10px; /* Add some margin between cards */
            background-color: white;
            border-radius: 10px;
            box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
            overflow: hidden;
        }

        .card-image {
            width: 100%;
            height: 200px;
            background-size: cover;
            background-position: center;
        }

        .card-content {
            padding: 10px;
        }

        .card-actions {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0 10px 10px 10px;
        }

        .btn {
            padding: 10px 20px;
            border-radius: 5px;
            text-decoration: none;
            color: white;
            cursor: pointer;
        }

        .btn-primary {
            background-color: #007bff;
        }

        .btn-secondary {
            background-color: #6c757d;
        }

        .btn-danger {
            background-color: #dc3545;
        }
    </style>
</head>
<body>
    <div class="exercise-content">
        <div class="card">
            <div class="card-image" style="background-image: url('@Url.Content($"~/Images/{Model.ExerciseId}.jpg")')"></div>
            <div class="card-content">
                <h2>@Model.Name</h2>
                <p>@Model.Instruction</p>
            </div>
            <div class="card-actions">
                <a asp-action="Edit" asp-route-id="@Model.ExerciseId" class="btn btn-primary">Edit</a>
                <a asp-action="Details" asp-route-id="@Model.ExerciseId" class="btn btn-secondary">Details</a>
                <a asp-action="Delete" asp-route-id="@Model.ExerciseId" class="btn btn-danger">Delete</a>
            </div>
        </div>
    </div>
</body>
</html>
